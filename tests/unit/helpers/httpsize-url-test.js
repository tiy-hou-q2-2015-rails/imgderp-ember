import { httpsizeUrl } from '../../../helpers/httpsize-url';
import { module, test } from 'qunit';

module('Unit | Helper | httpsize url');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = httpsizeUrl(42);
  assert.ok(result);
});
